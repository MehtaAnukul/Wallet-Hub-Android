<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.ImageUploadActivity"
    android:orientation="vertical">

    <include layout="@layout/toolbar_layout"/>

    <Spinner
        android:id="@+id/activity_musicUpload_labelSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp">
    </Spinner>

    <Button
        android:id="@+id/activity_musicUpload_selectMusicBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="30dp"
        android:layout_marginRight="30dp"
        android:layout_marginTop="40dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="30dp"
        android:text="@string/selectMusic"
        android:textColor="@color/colorWhite"
        android:background="@drawable/bg"/>

    <Button
        android:id="@+id/activity_musicUpload_uploadBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="80dp"
        android:layout_marginRight="80dp"
        android:layout_marginTop="40dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="30dp"
        android:text="@string/musicUpload"
        android:textColor="@color/colorWhite"
        android:background="@drawable/bg"/>
</LinearLayout>